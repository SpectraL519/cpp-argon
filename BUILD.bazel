load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "cpp-argon",
    hdrs = glob(["include/**/*.hpp"]),
    includes = ["include"],
    cxxopts = ["-std=c++20"],
    visibility = ["//visibility:public"],
)
